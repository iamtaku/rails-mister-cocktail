<div class="container">
  <div class="cocktail-card-title">
    <h1><%= @cocktail.name %></h1>
    <%= link_to 'Add a Dose', new_cocktail_dose_path(@cocktail) %>
  </div>
  <div class="cocktails">
    <div class="cocktail-description">
      <h3>Description:</h3>
      <p>lorem ipsum</p>
    </div>
    <div class="cocktail-doses">
      <table class="table table-dark ">
        <thead>
          <tr>
            <th scope="col">Amount</th>
            <th scope="col">Ingredients</th>
          </tr>
        </thead>
        <tbody class="table-striped tbody-dark">
         <% @doses.each do |dose| %>
         <tr>
          <th><p><%= dose.description %></p> </th>
          <th><p><%= dose.ingredient.name %></p></th>
          <th><%= link_to '🗑', dose_path(dose), method: :delete, class: 'trash'  %></th>
         </tr>
         </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>
